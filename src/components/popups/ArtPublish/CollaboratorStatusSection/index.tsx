import styles from './style.module.scss';

const CollaboratorStatusSection = () => (
  <div className={styles.root}>
    <h4 className={styles.root__title}>Status</h4>
  </div>
);

export default CollaboratorStatusSection;
