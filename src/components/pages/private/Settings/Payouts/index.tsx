import { useEffect } from 'react';

const Payouts = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);

  return (
    <div>
      <h1>Payouts</h1>
    </div>
  );
};

export default Payouts;
